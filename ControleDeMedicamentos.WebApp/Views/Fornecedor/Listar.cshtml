@model List<Fornecedor>

@{
    Layout = "_Layout";
    ViewBag.Titulo = "Listagem de Fornecedor";
}

<p>
    <a class="btn btn-primary" href="Cadastrar">Cadastrar fornecedor</a>
    <p />

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nome</th>
                <th>Telefone</th>
                <th>CNPJ</th>
                <th>Ações</th>
            </tr>
        </thead>

        <tbody>
            @foreach (Fornecedor f in Model)
            {
                <tr>
                    <td>@f.Id</td>
                    <td>@f.Nome</td>
                    <td>@f.Telefone</td>
                    <td>@f.Cnpj</td>
                    <td>
                        <a class="btn btn-sm btn-outline-primary" href="Editar/@f.Id">Editar</a>
                        <a class="btn btn-sm btn-outline-danger" href="Excluir/@f.Id">Excluir</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>